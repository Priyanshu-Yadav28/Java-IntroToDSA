package oop2;

//Inner class = class inside a class.
public class InnerClasses { // outside class cannot be static if declared error will be
    // displayed . Only Inner classes can be static
    class Test { // here we can put static or remove it

    }
}
